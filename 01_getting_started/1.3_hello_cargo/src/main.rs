fn main() {
    // cargo new proj: create a new project
    // cargo new proj --vcs none: create new project without creating a git rep
    // cargo run: run project
    // cargo build: compile without optimizations
    // cargo build --release: compile with optimizations
    println!("Hello, cargo!");
}
