// A package can contain multiple binary crates and optionally one library crate
// Binary crates must have a main function
// Library crates don't have a main function
// Library crates define functionality intended to be shared with multiple projects.
fn main() {
    println!("Hello, packages, crates modules and paths!");
}
